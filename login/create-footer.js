const footer = document.createElement(tagName:'div');
const buttonFooter = document.createElement(tagName:'button');
buttonFooter.innerText = 'Login';
footer.appendChild = (buttonFooter);
const footerText = document.createTextnode(data:'hello world!');
footer.appendChild(footerText);
document.body.appendChild(footer);
console.log(footer);



console.log(footer.style);
