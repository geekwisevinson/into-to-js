const header = document.createElement( tagName: 'div');
-button.innerText = 'push me';
header.appendChild(button);
condy headerText = document.createTextNode( data: 'Hellow World!');
header.appendChild(headerText);
document.body.appendChild(header);
cosole.log(header);

const header.Style = {
      font: '24px',
      color: 'blue',
};

  header.style.fontSize =headerStyle.fontSize;
  header.style.color = headerStyle.color;

  console.log(header.style);
